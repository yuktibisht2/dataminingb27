_schema-version: "3.2"
ID: appdataminingb27
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: appdataminingb27-destination-content
  type: com.sap.application.content
  requires:
  - name: appdataminingb27-destination-service
    parameters:
      content-target: true
  - name: appdataminingb27-repo-host
    parameters:
      service-key:
        name: appdataminingb27-repo-host-key
  - name: appdataminingb27-uaa
    parameters:
      service-key:
        name: appdataminingb27-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: appdataminingb27_html_repo_host
          ServiceInstanceName: appdataminingb27-html5-service
          ServiceKeyName: appdataminingb27-repo-host-key
          sap.cloud.service: appdataminingb27
        - Authentication: OAuth2UserTokenExchange
          Name: appdataminingb27_uaa
          ServiceInstanceName: appdataminingb27-xsuaa-service
          ServiceKeyName: appdataminingb27-uaa-key
          sap.cloud.service: appdataminingb27
        existing_destinations_policy: update
  build-parameters:
    no-source: true
- name: appdataminingb27-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: appdataminingb27-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - appdataminingb27.zip
      name: appdataminingb27
      target-path: resources/
- name: appdataminingb27
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: appdataminingb27-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: appdataminingb27-destination-service
    service-plan: lite
- name: appdataminingb27-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: appdataminingb27-xsuaa-service
    service-plan: application
- name: appdataminingb27-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: appdataminingb27-html5-service
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
